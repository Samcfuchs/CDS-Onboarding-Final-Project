{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch model",
            "webRoot": "${workspaceFolder}",
            "url": "http://localhost:8000",
        }
    ]
}
